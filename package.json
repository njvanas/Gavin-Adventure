{
  "name": "gavin-adventure",
  "private": true,
  "description": "Gavin Adventure — gym-themed canvas platformer (GitHub Pages static bundle)",
  "scripts": {
    "start": "npx http-server . -p 8000 -c-1",
    "dev": "npx http-server . -p 8000 -c-1 -o"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
